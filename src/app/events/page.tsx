import Link from "next/link";import {events} from "@/lib/events";
export default function EventsPage(){return <div className="page"><header className="page-header"><div><p className="muted">All Projects</p><h1 className="page-title">Events</h1><p className="page-description">管理所有活動、專案與執行進度。</p></div><Link className="button button-primary" href="/events/new">＋ New Event</Link></header><section className="events-grid">{events.map(e=><Link href={`/events/${e.slug}`} className="event-card" key={e.slug}><span className="event-status">{e.status}</span><h2>{e.name}</h2><div className="event-meta"><span>{e.subtitle}</span><span>{e.date} · {e.location}</span></div><div className="event-progress"><div className="progress-row"><span className="muted">Progress</span><div className="progress-track"><div className="progress-fill" style={{width:`${e.progress}%`}}/></div><strong>{e.progress}%</strong></div></div></Link>)}</section></div>}
